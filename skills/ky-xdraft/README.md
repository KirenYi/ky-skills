# ky-xdraft

把本地 Markdown 做成 **X 长文草稿** 的流程 skill（默认不发布）。

## 核心

检查 Markdown → 指导你在 X 上保存草稿。

## 不是什么

- 不是 X 官方插件  
- 不替你自动点「发布」  
- 本目录默认不附带从别处拷来的上传器实现  

## 用法

```text
/ky-xdraft
检查并帮我把 article.md 弄成 X 草稿
```

详见 `SKILL.md`。
